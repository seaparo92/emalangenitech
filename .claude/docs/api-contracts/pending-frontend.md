# Pending Frontend Implementation

> Auto-generated by Claude's `api-docs-sync` skill
> Last updated: 2026-01-28

This document lists API endpoints that the **Backend provides** but the **Frontend hasn't implemented yet**.

**Frontend developers**: Review this document and implement the API integrations.

---

## Summary

| Priority | Count |
|----------|-------|
| CRITICAL | 0 |
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 0 |

**Total Pending: 0**

---

## Available Endpoints

*No pending frontend implementations at this time.*

*When Claude detects new backend endpoints that the frontend should use, they will be documented here automatically.*

---

## Recently Completed

*Completed items are moved here for 7 days before being archived.*

---

## Template (for reference)

When a new endpoint is documented, it follows this format:

```markdown
### [Feature] - [Endpoint Description]

**Provided by:** Backend change in `[file:line]`
**Date:** YYYY-MM-DD
**Priority:** CRITICAL | HIGH | MEDIUM | LOW
**Status:** PENDING

#### Available Endpoint

| Method | Path | Auth Required |
|--------|------|---------------|
| METHOD | /api/path | Yes/No |

#### How to Call

\`\`\`typescript
async function methodName(): Promise<ResponseType> {
  const response = await api.get<ResponseType>('/api/path')
  return response.data
}
\`\`\`

#### Response Type

\`\`\`typescript
interface ResponseType {
  field: type
}
\`\`\`

#### Example Response

\`\`\`json
{
  "field": "value"
}
\`\`\`

#### Error Handling

\`\`\`typescript
try {
  const data = await methodName()
} catch (error) {
  // Handle specific errors
}
\`\`\`

#### Suggested Implementation

1. Add service method in `src/services/[feature].service.ts`
2. Create hook if needed
3. Use in component

#### Backend Context

Why this endpoint was created and any relevant notes.
```

---

*This document is auto-maintained. Add [MANUAL] tag to preserve custom notes.*
